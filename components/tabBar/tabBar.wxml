<t-tab-bar t-class="t-tab-bar" value="{{active}}" split="{{false}}" bindchange="onChange">
    <t-tab-bar-item wx:for="{{routeList}}" value="{{item.name}}" icon="{{item.icon}}">{{item.title}}</t-tab-bar-item>
</t-tab-bar>
